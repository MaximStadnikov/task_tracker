<h1>Tasks</h1>

<ul>
    <% @tasks.each do |task| %>
        <li>
            <%= link_to task.name, url_for([@project, task]) %>
        </li>
     
    <% end %>
</ul>

<%= link_to "Create Task", new_project_task_path %>
<%= link_to "Back", @project %>
